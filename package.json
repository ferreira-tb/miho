{
  "name": "miho",
  "version": "5.0.4",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@8.15.5",
  "scripts": {
    "mock:bump": "cargo run -- bump -t -p mocks",
    "mock:update": "cargo run -- update -t -p mocks"
  }
}